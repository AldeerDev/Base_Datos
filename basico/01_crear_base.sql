-- En SQLite, la base de datos se crea al abrir un archivo nuevo
-- Este script no crea la base en si, pero sirve como punto de partida

-- Comando desde terminal para crear una base nueva:
-- sqlite3 mi_base.db

-- Luego podes correr los demas scripts sobre "mi_base.db"
